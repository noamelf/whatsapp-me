# WhatsApp Bot Configuration

# SINGLE TENANT MODE (backward compatible)
# ========================================
# Use these environment variables for single tenant deployment
# If tenants.json exists, it will be used instead

# Target group configuration - use either TARGET_GROUP_ID or TARGET_GROUP_NAME
# If TARGET_GROUP_ID is provided, it will be used directly
# If only TARGET_GROUP_NAME is provided, the bot will search for the group by name
TARGET_GROUP_ID=
TARGET_GROUP_NAME=אני

# OpenRouter API Key (required)
# Get your key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# LLM Model to use (defaults to free Gemini)
LLM_MODEL=google/gemini-2.0-flash-exp:free

# Fallback model for rate limits (defaults to free Llama)
LLM_FALLBACK_MODEL=meta-llama/llama-3.3-70b-instruct:free

# Test Endpoint Authentication Token (optional, but recommended for production)
# Generate a secure token with: openssl rand -hex 32
# If set, the /test-message endpoint requires this token in the Authorization header
# Example: curl -H "Authorization: Bearer YOUR_TOKEN" ...
TEST_ENDPOINT_TOKEN=

# Optional: Filter which chats to analyze (comma-separated list)
# Leave empty to analyze all chats
ALLOWED_CHAT_NAMES=

# MULTI TENANT MODE
# ==================
# For multi-tenant deployment, create a tenants.json file in the project root
# See tenants.json.example for the structure
# When tenants.json exists, environment variables above are ignored (except OPENROUTER_API_KEY)
